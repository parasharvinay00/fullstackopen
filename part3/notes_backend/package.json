{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && npm --prefix ../../part2/notes run build && cp -r ../../part2/notes/dist .",
    "deploy:full": "npm run build:ui && git -C ../.. add part2/notes/src part3/notes_backend && git -C ../.. add -f part3/notes_backend/dist && git -C ../.. commit -m \"uibuild\" && git -C ../.. push"
  },
  "keywords": [],
  "author": "Vinay Parashar",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^8.24.1"
  }
}
